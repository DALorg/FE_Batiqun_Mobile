/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['batiqunapimysql20220624114651.azurewebsites.net'],
  }
};

module.exports = nextConfig;
